# Disposable local PostgreSQL used by Docker Compose and local development tools.
POSTGRES_USER=strengthsync_local
POSTGRES_PASSWORD=strengthsync_local_dev
POSTGRES_DB=strengthsync_local
POSTGRES_PORT=5432
DATABASE_URL=postgresql://strengthsync_local:strengthsync_local_dev@127.0.0.1:5432/strengthsync_local

# Local application configuration.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=local-development-only-secret

# Optional integrations. Leave empty until a local workflow needs them.
OPENAI_API_KEY=
OPENAI_ORG_ID=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
TEAMS_WEBHOOK_URL=
MICROSOFT_APP_ID=
MICROSOFT_APP_PASSWORD=
MICROSOFT_APP_TENANT_ID=
CRON_SECRET=

# Platform operators are explicit existing immutable user IDs, comma-separated.
STRENGTHSYNC_PLATFORM_ADMIN_USER_IDS=
# Bind the optional Teams webhook fallback to exactly one organization.
TEAMS_WEBHOOK_ORGANIZATION_ID=
# Unset by default. Configure only a header overwritten by a trusted ingress.
STRENGTHSYNC_TRUSTED_CLIENT_IP_HEADER=
